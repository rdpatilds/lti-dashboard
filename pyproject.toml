[project]
name = "lti-dashboard"
version = "0.1.0"
description = "Minimal LTI 1.3 tool showing Redshift history and live Canvas state side by side"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "pyjwt[crypto]",
    "boto3>=1.34",
    "python-multipart",
]
